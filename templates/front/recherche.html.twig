{% extends 'base.html.twig' %}

{% block stylesheets %}
    <!-- ICONSCOUT CDN -->
    <link rel="stylesheet" href="https://unicons.iconscout.com/release/v2.1.6/css/unicons.css">
    <!-- STYLESHEET -->
    <link rel="stylesheet" href="{{ asset('frontoffice/css/style.css') }}">
    <link rel="stylesheet" href="{{ asset('frontoffice/css/form.css') }}">
{% endblock %}


{% block middle %}

    <h1>Résultats de la recherche pour "{{ query }}"</h1>

    {% if actualites is empty %}
        <p>Aucun résultat trouvé.</p>
    {% else %}
        <ul>
            {% for actualite in actualites %}
                <li>
                    <article class="brick entry format-standard animate-this">
                        <div class="entry-thumb">
                            <a href="{{ path('app_ajouterCom', {'id': actualite.id}) }}" class="thumb-link">
                                <img src="{{ asset('uploads/images/'~ actualite.image ) }}" alt="">
                            </a>
                        </div>
                        <div class="entry-text">
                            <div class="entry-header">
                                <div class="entry-meta">
                <span class="cat-links">
                    {{ actualite.categorie }}
                </span>
                                </div>
                                <h1 class="entry-title">
                                    <a href="{{ path('app_ajouterCom', {'id': actualite.id}) }}">
                                        {{ actualite.titre }}
                                    </a>
                                </h1>
                            </div>
                            <div class="entry-excerpt">
                                {{ actualite.description }}
                            </div>
                            <p>{{ actualite.date|date("F jS \\a\\t g:ia") }}</p>

                        </div>
                    </article>
                </li>
            {% endfor %}
        </ul>
    {% endif %}
{% endblock %}

{% block javascripts %}
{% endblock %}
