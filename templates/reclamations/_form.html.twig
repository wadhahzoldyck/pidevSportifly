{{ form_start( reclamation,{'attr': {'class':'form-horizontal','novalidate':'novalidate'}} ) }}
    {{ form_widget(reclamation) }}
<button class="btn">{{ button_label|default('Save') }}</button>

{{ form_end(reclamation) }}
